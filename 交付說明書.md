# 專案交付說明書

**交付日期：** 2026-03-04
**版本：** v1.1（已依審查意見修正）

---

## 一、專案概覽

本次交付包含 **兩個獨立系統**：

| 系統 | 技術 | 功能 |
|------|------|------|
| Stone Garden Podcast Classifier | Python + Streamlit | 上傳 MP3 → 自動分析音頻 → 分類 → 給出短影音建議 |
| Attention Matrix v1.0-alpha（基礎骨架已建立，待工程師完善） | Node.js | 自動監控 Threads/RSS → AI 分析 → Telegram 推播通知 |

---

## 二、系統 A：Podcast 分類器

### 功能說明
1. 上傳 MP3 / WAV / M4A 音頻檔案
2. 系統自動分析音頻特徵（能量、節奏、音調）
3. 自動分類為 4 種類型：
   - 能量激勵型 → 建議 IG Reels（15-20秒）
   - 心靈療癒型 → 建議 Story/Shorts（12-15秒）
   - 冥想正念型 → 建議 TikTok（20-30秒）
   - 知識教學型 → 建議 YouTube Shorts（30秒）

### 部署方式

#### 方法 1：Streamlit Cloud（免費、推薦）
1. 登入 https://share.streamlit.io
2. 點擊「New app」
3. 連結 GitHub repo：`sodfhiuewfhiuw/stone-garden-podcast-classifier`
4. 主檔案設定：`app.py`
5. 點擊「Deploy」完成！

#### 方法 2：本地運行
```bash
git clone https://github.com/sodfhiuewfhiuw/stone-garden-podcast-classifier.git
cd stone-garden-podcast-classifier
pip install -r requirements.txt
streamlit run app.py
```
開啟瀏覽器前往：http://localhost:8501

#### 方法 3：Docker
```bash
docker build -t podcast-classifier .
docker run -p 8501:8501 podcast-classifier
```

### 環境需求
- Python 3.8+
- 套件見 `requirements.txt`（streamlit, librosa, numpy, soundfile）
- **無需** API key，完全離線運行

---

## 三、系統 B：Attention Matrix v1.0-alpha

### 功能說明
基於 OODA Loop（觀察 → 判斷 → 決策 → 行動）四層架構：
- **監控 Threads** 關鍵字（台中徵才、台中招聘等）每 2 小時自動掃描
- **AI 自動分析**每篇文章，提取 email / 電話 / 薪資
- **評分過濾**（有 email +20分、有電話 +10分、有薪資 +15分，≥20分才推送）
- **Telegram 推播**通知，24小時去重防重複

### 安裝與啟動
```bash
# 安裝依賴
npm install

# 設定環境變數（見下方）
cp .env.example .env
# 編輯 .env 填入你的 token

# 啟動系統
npm start
```

### 必填環境變數

請建立 `.env` 檔案，填入以下三個值：

```env
TELEGRAM_BOT_TOKEN=你的Telegram機器人Token
TELEGRAM_CHAT_ID=你的Telegram聊天室ID
AI_API_KEY=你的AI_API金鑰
```

| 變數 | 說明 | 取得方式 |
|------|------|---------|
| `TELEGRAM_BOT_TOKEN` | Telegram 機器人 Token | 在 Telegram 找 @BotFather，輸入 /newbot |
| `TELEGRAM_CHAT_ID` | 接收通知的聊天室 ID | 找 @userinfobot 取得你的 ID |
| `AI_API_KEY` | AI 分析用 API 金鑰 | 至 https://aiapi.clzjwl.cn/ 註冊取得 |

### 常用指令
```bash
npm start          # 啟動系統
npm run status     # 查看任務統計（讀取 SQLite，可在系統運行中執行）
npm run reload     # 熱重載設定（觸發 config 變更，不重啟）
npm test           # 執行 Jest 測試
npm run test:coverage  # 產出測試覆蓋率報告
```

### 自訂監控關鍵字
編輯 `config/matrix.yaml`，找到 `keywords` 欄位修改：
```yaml
keywords: ["台中徵才", "台中招聘", "台中工作"]
```
支援新增任意關鍵字，儲存後執行 `npm run reload` 即時生效。

---

## 三之一、功能實現狀態

| 項目 | 狀態 | 說明 |
|------|------|------|
| `npm start` | ✅ 完成 | 正常啟動 |
| `npm run status` | ✅ 完成 | 讀取 SQLite 輸出任務統計 |
| `npm run reload` | ✅ 完成 | touch config 觸發 chokidar 熱重載 |
| `npm test` | ✅ 完成 | Jest 三個測試檔，涵蓋 analyzer / notifier / e2e |
| 斷路器（Circuit Breaker） | ✅ 完成 | `core/circuit-breaker.js`，5次失敗自動斷開，60s後嘗試恢復 |
| AI 失敗正則兜底 | ✅ 完成 | `analyzer.js` 斷路器開啟時自動切換 regex 萃取 |
| 健康監控 | ✅ 完成 | 每60s記錄記憶體用量，超過閾值發出警告 |
| RSS 連接器 | ✅ 完成 | `connectors/rss.js`，支援 RSS/Atom/JSON Feed |
| 熱重載配置 | ✅ 完成 | chokidar 監聽 matrix.yaml，自動新增/移除任務 |
| 測試覆蓋 | ✅ 完成 | analyzer / notifier / e2e 全覆蓋 |

---

## 四、檔案結構說明

```
stone-garden-podcast-classifier/
├── app.py                    # Podcast 分類器主程式
├── requirements.txt          # Python 套件清單
├── Dockerfile                # Docker 部署設定
├── index.js                  # Attention Matrix 入口
├── package.json              # Node.js 套件清單
├── config/
│   └── matrix.yaml           # ★ 主要設定檔（關鍵字、排程、AI模型）
├── core/
│   ├── engine.js             # OODA 核心引擎
│   ├── analyzer.js           # AI 分析模組
│   ├── database.js           # SQLite 資料庫
│   ├── notifier.js           # Telegram 通知
│   └── task.js               # 任務排程
├── connectors/
│   ├── threads.js            # Threads 爬蟲
│   └── rss.js                # RSS 訂閱源
└── scripts/
    ├── reload.js             # 熱重載工具
    ├── status.js             # 狀態查詢
    └── resend.js             # 重發通知
```

---

## 五、常見問題

**Q：Podcast 分類器準確度不高？**
A：目前使用音頻特徵規則分類（能量/節奏/音調），若需 AI 模型分類可告知升級。

**Q：Attention Matrix 沒有收到 Telegram 通知？**
A：請確認三個環境變數都已正確填寫，並確認 Bot 已加入目標群組或頻道。

**Q：如何新增 RSS 訂閱源？**
A：在 `config/matrix.yaml` 新增一個 task，將 `connector` 設為 `rss`，並填入 `feed_url`。

**Q：資料存在哪裡？**
A：SQLite 資料庫存於 `data/matrix.db`（自動建立）。

---

## 六、技術支援

如有問題請提供：
1. 錯誤訊息截圖
2. `npm run status` 輸出結果
3. 使用的作業系統與 Node.js 版本（`node -v`）
